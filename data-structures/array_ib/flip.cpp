vi flip(string);

int sumString(string str, int l, int r) {
    int count = 0;
    while(l < r) if (str[l++] == '1') count++;
    return count;
}

vi flip(string A) {
    vi V;

    int begin = -1, end = -1, sum = 0, final_sum = 0, beg = 0, end = 0;

    for (int i = 0; i < A.length(); i++) {
        
    }
    

}